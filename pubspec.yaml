name: crm_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.5.1

  # Routing
  auto_route: ^7.8.4

  # DI
  get_it: ^7.6.7
  injectable: ^2.4.1

  # Data Class / Serialization
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  equatable: ^2.0.5
  uuid: ^4.3.3

  # Network
  dio: ^5.4.1

  # Local Storage
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  flutter_secure_storage: ^9.0.0
  path: ^1.9.0

  # UI / Utils
  google_fonts: ^6.1.0
  intl: ^0.20.2
  gap: ^3.0.0
  flutter_svg: ^2.0.10
  open_file: ^3.3.2
  permission_handler: ^11.3.0
  
  # Features
  table_calendar: ^3.1.0
  fl_chart: ^0.66.2 # Charts alternative
  
  # PDF
  pdf: ^3.10.7
  printing: ^5.11.1
  excel: ^4.0.0
  
  # Speech & AI
  speech_to_text: ^7.0.0
  # openai_dart: ^0.1.0 # Or use Dio directly for flexibility

  # Google / Auth
  google_sign_in: ^6.2.1
  googleapis: ^13.1.0
  googleapis_auth: ^1.6.0
  extension_google_sign_in_as_googleapis_auth: ^2.0.7
  url_launcher: ^6.2.5
  image_picker: ^1.2.1
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.0
  local_auth: ^2.2.0
  share_plus: ^12.0.1
  signature: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  
  # Code Gen
  build_runner: ^2.4.8
  riverpod_generator: ^2.4.0
  auto_route_generator: ^7.3.2
  injectable_generator: ^2.6.1
  freezed: ^2.4.7
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  generate: true
